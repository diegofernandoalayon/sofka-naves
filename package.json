{
  "name": "reto_sofka",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "workspaces": [
    "api",
    "app"
  ],
  "engines":{
    "npm":"8.0.0"
  },
  "main": "index.js",
  "scripts": {
    "build": "npm run build --workspace=app",
    "start": "npm run start --workspace=api",
    "dev:api": "npm run dev --workspace=api",
    "dev:app":"npm run start --workspace=app",
    "dev:full": "npm run build && npm run dev --workspace=api"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "app": "^0.1.0",
    "create-react-app": "^5.0.1"
  },
  "devDependencies": {
    "jest": "^28.1.3"
  }
}
